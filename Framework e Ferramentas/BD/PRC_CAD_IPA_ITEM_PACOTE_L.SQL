--PRC_CAD_IPA_ITEM_PACOTE_L
create or replace procedure PRC_CAD_IPA_ITEM_PACOTE_L 
(
     pCAD_IPA_ID IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_ID%type DEFAULT NULL,
     pCAD_PRD_ID_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_PRD_ID_PACOTE%type DEFAULT NULL,
     pCAD_IPA_VL_ITEM_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_ITEM_PACOTE%type DEFAULT NULL,
     pCAD_IPA_PC_RATEIO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_PC_RATEIO%type DEFAULT NULL,
     pCAD_IPA_DT_INICIO_VIGENCIA IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_DT_INICIO_VIGENCIA%type DEFAULT NULL,
     pCAD_IPA_DT_FIM_VIGENCIA IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_DT_FIM_VIGENCIA%type DEFAULT NULL,
     pSEG_USU_ID_USUARIO IN TB_CAD_IPA_ITEM_PACOTE.SEG_USU_ID_USUARIO%type DEFAULT NULL,
     pCAD_IPA_FL_STATUS IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_FL_STATUS%type DEFAULT NULL,
     pCAD_IPA_PC_DESCONTO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_PC_DESCONTO%type DEFAULT NULL,
     pCAD_IPA_PC_ACRESCIMO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_PC_ACRESCIMO%type DEFAULT NULL,
     pCAD_IPA_DS_OBSERVACAO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_DS_OBSERVACAO%type DEFAULT NULL,
     pCAD_IPA_DT_ULTIMA_ATUALIZACAO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_DT_ULTIMA_ATUALIZACAO%type DEFAULT NULL,
     pCAD_PRD_ID_ITEM_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_PRD_ID_ITEM_PACOTE%type DEFAULT NULL,
     pCAD_IPA_VL_ORIG_ITEM_PRODUTO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_ORIG_ITEM_PRODUTO%type DEFAULT NULL,
     pCAD_IPA_VL_ACRESCIMO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_ACRESCIMO%type DEFAULT NULL,
     pCAD_IPA_VL_DESCONTO IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_DESCONTO%type DEFAULT NULL,
     pCAD_IPA_VL_FINAL_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_FINAL_PACOTE%type DEFAULT NULL,
     pCAD_IPA_QT_ITEM_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_QT_ITEM_PACOTE%type DEFAULT NULL,
     pCAD_IPA_NR_DIVISOR_PACOTE IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_NR_DIVISOR_PACOTE%type DEFAULT NULL,
     pCAD_IPA_VL_PROPORCIONAL IN TB_CAD_IPA_ITEM_PACOTE.CAD_IPA_VL_PROPORCIONAL%type DEFAULT NULL,
     io_cursor OUT PKG_CURSOR.t_cursor
) 
is
/********************************************************************
*    Procedure: PRC_CAD_IPA_ITEM_PACOTE_L
* 
*    Data Criacao: 	data da  criação   Por: Nome do Analista
*    Data Alteracao:	data da alteração  Por: Nome do Analista
*
*    Funcao: Descrição da funcionalidade da Stored Procedure
*
*******************************************************************/
 v_cursor PKG_CURSOR.t_cursor;
  V_WHERE  varchar2(5000);
  V_SELECT  varchar2(5000);
begin
  V_WHERE := NULL;
  IF pCAD_IPA_ID IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_ID = ' || pCAD_IPA_ID; END IF;
IF pCAD_PRD_ID_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_PRD_ID_PACOTE = ' || pCAD_PRD_ID_PACOTE; END IF;
IF pCAD_IPA_VL_ITEM_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_ITEM_PACOTE = ' || pCAD_IPA_VL_ITEM_PACOTE; END IF;
IF pCAD_IPA_PC_RATEIO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_PC_RATEIO = ' || pCAD_IPA_PC_RATEIO; END IF;
IF pCAD_IPA_DT_INICIO_VIGENCIA IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_DT_INICIO_VIGENCIA = ' || CHR(39) || pCAD_IPA_DT_INICIO_VIGENCIA || CHR(39); END IF;
IF pCAD_IPA_DT_FIM_VIGENCIA IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_DT_FIM_VIGENCIA = ' || CHR(39) || pCAD_IPA_DT_FIM_VIGENCIA || CHR(39); END IF;
IF pSEG_USU_ID_USUARIO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND SEG_USU_ID_USUARIO = ' || pSEG_USU_ID_USUARIO; END IF;
IF pCAD_IPA_FL_STATUS IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_FL_STATUS = ' || CHR(39) || pCAD_IPA_FL_STATUS || CHR(39); END IF;
IF pCAD_IPA_PC_DESCONTO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_PC_DESCONTO = ' || pCAD_IPA_PC_DESCONTO; END IF;
IF pCAD_IPA_PC_ACRESCIMO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_PC_ACRESCIMO = ' || pCAD_IPA_PC_ACRESCIMO; END IF;
IF pCAD_IPA_DS_OBSERVACAO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_DS_OBSERVACAO = ' || CHR(39) || pCAD_IPA_DS_OBSERVACAO || CHR(39); END IF;
IF pCAD_IPA_DT_ULTIMA_ATUALIZACAO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_DT_ULTIMA_ATUALIZACAO = ' || CHR(39) || pCAD_IPA_DT_ULTIMA_ATUALIZACAO || CHR(39); END IF;
IF pCAD_PRD_ID_ITEM_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_PRD_ID_ITEM_PACOTE = ' || pCAD_PRD_ID_ITEM_PACOTE; END IF;
IF pCAD_IPA_VL_ORIG_ITEM_PRODUTO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_ORIG_ITEM_PRODUTO = ' || pCAD_IPA_VL_ORIG_ITEM_PRODUTO; END IF;
IF pCAD_IPA_VL_ACRESCIMO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_ACRESCIMO = ' || pCAD_IPA_VL_ACRESCIMO; END IF;
IF pCAD_IPA_VL_DESCONTO IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_DESCONTO = ' || pCAD_IPA_VL_DESCONTO; END IF;
IF pCAD_IPA_VL_FINAL_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_FINAL_PACOTE = ' || pCAD_IPA_VL_FINAL_PACOTE; END IF;
IF pCAD_IPA_QT_ITEM_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_QT_ITEM_PACOTE = ' || pCAD_IPA_QT_ITEM_PACOTE; END IF;
IF pCAD_IPA_NR_DIVISOR_PACOTE IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_NR_DIVISOR_PACOTE = ' || pCAD_IPA_NR_DIVISOR_PACOTE; END IF;
IF pCAD_IPA_VL_PROPORCIONAL IS NOT NULL THEN V_WHERE:= V_WHERE || ' AND CAD_IPA_VL_PROPORCIONAL = ' || pCAD_IPA_VL_PROPORCIONAL; END IF;
 
   V_SELECT := '
SELECT	
       CAD_IPA_ID,
       CAD_PRD_ID_PACOTE,
       CAD_IPA_VL_ITEM_PACOTE,
       CAD_IPA_PC_RATEIO,
       CAD_IPA_DT_INICIO_VIGENCIA,
       CAD_IPA_DT_FIM_VIGENCIA,
       SEG_USU_ID_USUARIO,
       CAD_IPA_FL_STATUS,
       CAD_IPA_PC_DESCONTO,
       CAD_IPA_PC_ACRESCIMO,
       CAD_IPA_DS_OBSERVACAO,
       CAD_IPA_DT_ULTIMA_ATUALIZACAO,
       CAD_PRD_ID_ITEM_PACOTE,
       CAD_IPA_VL_ORIG_ITEM_PRODUTO,
       CAD_IPA_VL_ACRESCIMO,
       CAD_IPA_VL_DESCONTO,
       CAD_IPA_VL_FINAL_PACOTE,
       CAD_IPA_QT_ITEM_PACOTE,
       CAD_IPA_NR_DIVISOR_PACOTE,
       CAD_IPA_VL_PROPORCIONAL
FROM TB_CAD_IPA_ITEM_PACOTE
WHERE null is null  '    ;
       
OPEN v_cursor FOR
  V_SELECT || V_WHERE ;
  io_cursor := v_cursor;
end PRC_CAD_IPA_ITEM_PACOTE_L;
